{
  "name": "simple-http-proxy-gateway",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "dev:bun": "bun run --hot src/bun.ts",
    "start:bun": "bun run src/bun.ts",
    "dev:worker": "wrangler dev",
    "deploy:worker": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "bun-types": "^1.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
