# The token used for proxy authentication (required)
# The username is always hardcoded as "x-ray"
PROXY_AUTH_TOKEN=your_super_secret_token_here

# The port the Bun server will listen on (optional, defaults to 3000)
# PORT=3000
